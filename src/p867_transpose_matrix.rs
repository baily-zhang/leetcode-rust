use crate::Solution;

// @leet start
impl Solution {
    pub fn transpose(matrix: Vec<Vec<i32>>) -> Vec<Vec<i32>> {
        let cols = matrix[0].len();
        (0..cols).map(|j| {})
    }
}
// @leet end
